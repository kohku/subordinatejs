export const enum ProcessingEvent {
  Start = 'start',
  NextCommand = 'next-command',
  CommandComplete = 'command-complete',
  CommandFailed = 'command-failed',
  Failed = 'failed',
  Complete = 'complete',
}
